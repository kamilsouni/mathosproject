platform :ios, '13.0'

target 'Runner' do
  use_frameworks!
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  # Ajoutez d’autres pods si nécessaire, comme Firestore ou Analytics, par exemple :
  # pod 'Firebase/Firestore'
  # pod 'Firebase/Analytics'
end
